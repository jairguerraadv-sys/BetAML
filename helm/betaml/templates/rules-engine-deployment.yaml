{{- if .Values.rulesEngine.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "betaml.fullname" . }}-rules-engine
  labels:
    {{- include "betaml.labels" . | nindent 4 }}
    app.kubernetes.io/component: rules-engine
spec:
  replicas: {{ .Values.rulesEngine.replicaCount }}
  selector:
    matchLabels:
      {{- include "betaml.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: rules-engine
  template:
    metadata:
      labels:
        {{- include "betaml.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: rules-engine
    spec:
      containers:
        - name: rules-engine
          image: "{{ include "betaml.image" (dict "registry" .Values.global.imageRegistry "repository" .Values.rulesEngine.image.repository "tag" .Values.rulesEngine.image.tag "globalTag" .Values.global.imageTag) }}"
          imagePullPolicy: {{ .Values.rulesEngine.image.pullPolicy }}
          ports:
            - name: metrics
              containerPort: {{ .Values.rulesEngine.metricsPort }}
              protocol: TCP
            - name: health
              containerPort: {{ .Values.rulesEngine.healthPort }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health/live
              port: health
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health/ready
              port: health
            initialDelaySeconds: 20
            periodSeconds: 15
          resources:
            {{- toYaml .Values.rulesEngine.resources | nindent 12 }}
          envFrom:
            - configMapRef:
                name: {{ include "betaml.fullname" . }}-config
            - secretRef:
                name: {{ .Values.api.existingSecret }}
          env:
            - name: METRICS_PORT
              value: {{ .Values.rulesEngine.metricsPort | quote }}
            - name: HEALTH_PORT
              value: {{ .Values.rulesEngine.healthPort | quote }}
{{- end }}
